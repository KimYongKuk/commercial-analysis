# FastAPI 프레임워크와 서버
fastapi==0.115.5          # FastAPI 프레임워크 (메인!)
uvicorn[standard]==0.32.1  # ASGI 서버 (FastAPI를 실행하는 엔진)

# OpenAI API
openai==1.54.5            # OpenAI Python SDK

# 환경 변수 관리
python-dotenv==1.0.1      # .env 파일에서 API 키 읽기

# 데이터 검증
pydantic==2.10.3          # 데이터 검증 (FastAPI가 자동으로 사용)

# CORS (React 연결용)
# fastapi에 포함되어 있어서 별도 설치 불필요
